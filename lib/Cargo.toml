[package]
name = "snag-lib"
version = "0.1.0"
edition = "2024"
description = "Core library for snag bug reporter — bundle, detect, check, redact"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
